name: docker-escape-tool
version: 0.2.2

authors:
  - mil0 (mil0.io)

targets:
  docker-escape:
    main: src/docker-escape.cr

dependencies:
  docker:
    github: PercussiveElbow/docker-crystal
    branch: master
  net_sample:
    github: PercussiveElbow/net_sample.cr
    branch: master
# development_dependencies:
#   ameba:
#     github: crystal-ameba/ameba
#     version: ~> 0.13.0

crystal: 1.0.0 #targeting 1.0/nightly 

license: MIT
